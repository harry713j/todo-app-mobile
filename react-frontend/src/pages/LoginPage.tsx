import { Login } from "../components";

function LoginPage() {
  return (
    <div className="relative flex justify-center items-center w-full h-screen bg-sky-100">
      <Login />
    </div>
  );
}

export default LoginPage;
